<h1>My tasks</h1>
<ul>
  <% @tasks.each do |task|%>
    <li>
      <%= link_to task.title, task_path(task) %>
      <%= link_to task_path(task), data: {turbo_method: :delete, turbo_confirm: "Delete #{task.title}?"} do %>
        <%= tag.span class: %w[far fa-trash-alt] %>
      <% end %>
      </li>
  <% end %>
</ul>
<%= link_to "Add a new task", new_task_path %>
